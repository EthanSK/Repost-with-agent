# Editor / OS
.DS_Store
*.swp
*.swo
*.log

# Local backups produced during manual/agent-assisted migrations
*.bak.*

# Per-machine state (user data lives in ~/.repost-with-agent, not in the repo)
/.repost-with-agent/

# v3 build artefacts that should never come back
node_modules/
dist/
.env
*.js.map
*.d.ts

# Claude Code worktrees from agent runs (already managed elsewhere)
.claude/worktrees/
