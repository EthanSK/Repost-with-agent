# `/run`

Current safe run flow is preview plus history review:

```bash
repost-with-agent pair preview <id>
repost-with-agent pair history <id>
```

Do not claim a generic live run command exists yet.
