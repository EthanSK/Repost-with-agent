# <pair-id> learnings

_No learnings recorded yet — the agent will append entries as it discovers
quirks during runs._

<!--
Lifecycle (full spec: skills/repost-learnings/SKILL.md):

1. Start of every run: the agent reads this file as up-front context. It
   PRIORITIZES the most-recent entry's `### Selectors` and `### Step
   playbook` sections — those get tried verbatim BEFORE falling back to
   docs/destinations/<platform>.md.
2. End of every run: the agent appends any newly-discovered quirks below
   using the structured shape (free-form prose + optional structured
   sub-sections — see "Entry shape" below).
3. Append-only via `>>` in Bash. NEVER rewrite existing entries.
4. If a fresh observation contradicts an older entry, append a new entry
   AND add ` [obsoleted YYYY-MM-DD]` to the older heading via a targeted
   Edit. Don't delete history.

Entry shape (the three `###` sub-sections are OPTIONAL but strongly
preferred whenever the entry captures actionable mechanics — they let
future runs grep + skim for selectors and step playbooks instead of
re-reading prose):

    ## YYYY-MM-DD HH:MM — <one-line summary>

    <2–5 sentences of prose: what you saw, why it matters, implication.>

    ### Selectors
    - <label>: `<selector>` (<platform>, <where in flow>)
    - ...

    ### Step playbook
    1. <imperative step using the selectors above>
    2. ...

    ### Quirks
    - <one-line edge case / "skip if X" / timing note>

Save it (signal):
  - Persistent DOM selectors that worked, step playbooks future runs can
    follow verbatim, pagination caps, rate-limit signatures, shortener
    edge cases, account-specific behaviors, per-platform timing.
Don't save it (noise):
  - "Run worked" heartbeats, vague prose with no actionable detail
    ("LinkedIn was slow today"), one-off transient errors, anything
    already in docs/destinations/<platform>.md that you didn't actually
    observe a delta from, anything you're <75% sure is reproducible.

Example good entry (showing all three structured sections):

    ## 2026-05-12 14:22 — LinkedIn recent-activity pagination cap dropped to ~60

    Backfill run hit the end of `/recent-activity/all/` after ~60 posts
    loaded (was ~100 in the docs). Treat the "You're caught up" footer
    as a hard stop and emit `pair.fetch.exhausted`.

    ### Selectors
    - Profile feed list: `main ul.feed-shared-update-list` (linkedin, source scrape)
    - Each post item: `ul.feed-shared-update-list > li` (linkedin, source scrape)
    - Post permalink anchor: `a.update-components-actor__meta-link[href*="/feed/update/"]` (linkedin, per-post)
    - "You're caught up" footer: text="You're all caught up" (linkedin, end-of-feed marker)

    ### Step playbook
    1. Navigate to `<source.url>` (`/in/<handle>/recent-activity/all/`).
    2. Wait for `Profile feed list` to render.
    3. Scroll 1× and wait 600ms for new items to mount.
    4. For each `Each post item`, extract `Post permalink anchor`'s href + body text.
    5. Stop when `"You're caught up" footer` appears OR you have ≥`--max` non-dupes.
    6. Filter against `posted.jsonl`.

    ### Quirks
    - Feed virtualizes aggressively — scrape as you scroll, don't rely
      on all loaded posts staying in the DOM.
    - `lnkd.in` shorteners: leave as-is during scrape, expand at publish.
-->
